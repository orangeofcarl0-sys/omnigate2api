# omnigate2api — 发布记录

fork 自 HITZY2002/omnigate2api（华为云 CodeArts Agent / 码道 OpenAI 兼容代理）的
本地强化版。公开仓维护，未回馈上游。**单一真相源是 `docs/SPEC-adaptation-layer.md`**，
本文件只记每个版本"对外能感知到什么"。

## v1.4.0（当前；tag 待打）

- **双上游成熟**：腾讯客户端按域拆分（chat / 计费 / 活动 / 任务 / 埋点五域），全球版
  （www.workbuddy.ai）一等公民——域感知 base、国际版设备流登录、全球域首条 system 前置（§28.4 G3）
- **腾讯积分全自动（SPEC §32）**：每日签到（+100/天）+ 成长中心任务（当季 19 中 **17** 自动领奖）
  + 宠物探险循环（派出→归来领分→再派）+ 免费领养（+300c+8e）+ 能量开盲盒；事件链桌面 UA 门控；
  活动面故障与账号健康隔离。**全球域实证无活动体系且从网关侧打不通（§32.10）**，定位为聊天额度
- **裸模型名路由（SPEC §29）**：路由表热更 / 删除=禁用 / 面板「模型与路由」联合目录单表 +
  按区域付费/免费标注（`v3/config` 促销：日期窗 / 每日时段窗 / factor 判定）
- **标准供应商等价面**：`GET /v1/models`（+ `/{id}` 检索）、Anthropic 信封、`x-api-key`；
  **usage 上游真实值**（三协议 × 流式/非流式）+ 缓存命中与本次积分 `credit`（实测不破坏上游前缀缓存）
- **发送侧形态纪律（SPEC §33.4）**：官方客户端没有的控件默认值放行、非默认值 400 并点名违规键；
  客户端生成参数白名单透传（此前全部被静默丢弃）
- **治理**：模型级 `(账号, 模型)` 限流冷却、额度类 60s 软冷却不累计、传输层有界超时、
  `GOMEMLIMIT`/`GOGC` 内存边界、请求体上限 64→32MB
- **运维**：一键启动 `start.sh` / `start.cmd` / `panel.cmd`（引擎 → 指纹重建 → 容器 → 就绪）、
  桌面快捷入口、`OMNIGATE_DEBUG_CONFIG` 上游配置审计转储
- 华为通道 roles 化（SPEC §31）+ 双上游图片透传（SPEC §30）；修复原生 tool_calls 与正文流序颠倒

## v1.3.0

- 裸模型名路由 + WebUI「模型与路由」管理入口（SPEC §29）
- 腾讯每日自动签到 + 积分余额（`get-user-resource` 三层包裹解包修复；面板区分可花费余额与活动累计）
- 默认本地免密（空/`change-me` 视为未配置）+ 默认绑定 `127.0.0.1:7866`（开放方案文档化）
- 面板打磨：渠道徽章、调度卡真实字段、三协议 base 提示、控件视觉统一

## v1.2.0（公开仓库初始）

- 通用多上游 OpenAI 兼容适配层（声明化 Profile 体系）公开发布

## v1.0.0（本地增强版基线）

1. 登录链路修复: portal 二段握手(secret+redirect→code 下发)
2. 活动福利模型: maas_type 头 + 每日自动领取 + 网关 config/balance 封装
3. 真流式工具调用: 增量解析 + 围栏容错 + post 抑制 + 叙述即意图
4. 会话指纹续接(路线 D): 前缀哈希链 → tail 增量折叠,提示词 O(增量)
5. 稳定性: 取消豁免 / 429 软冷却 / finish_reason 补发 / UTF-8 边界修复
6. 观测: chat fold 日志 + TRANSCRIPT_ECHO 检测落盘 + agentdump 调试工具
7. 架构: chatCompletions 复杂度 63→41,测试覆盖围栏/指纹/限流/回声场景

## 部署

`bash start.sh`（或 Windows 双击 `start.cmd`；首次运行自动从 config.example.json 生成
config.json）——等价于 `docker compose up -d --build`。auths/ 放凭证；activity 模型需
账号有当日福利额度。

## 已验证

- 2026-08-30：pi / dsh 双客户端 16 轮长会话零回声零限流，增量折叠曲线恒定 ~3.6k；
  活动模型三只实测可用；纯流式/非流式/工具流式回归全绿
- 2026-09-24：腾讯积分链路活测（签到 +100/日、任务 claimed=16、宠物探险循环、领养 +300c+8e）
- 2026-09-25：全球域积分面审计收官（§32.10）；README/发布记录对齐当前实现
